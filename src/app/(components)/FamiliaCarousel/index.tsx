import React from 'react'
import EquipoCarousel from './EquipoCarousel'

type Props = {}

const FamiliaCarousel = (props: Props) => {
  return (
    <div className='container'>
        <EquipoCarousel />
    </div>
  )
}

export default FamiliaCarousel